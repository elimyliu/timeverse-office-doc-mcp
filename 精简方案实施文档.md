# MCP 工具精简实施文档

> 目标：将 93 个工具精简至 71 个，减少约 24% 的 token 开销。

## 一、变更总览

| 模块     | 原始     | 精简后    | 减少量     | 变更项数   |
| ------ | ------ | ------ | ------- | ------ |
| Word   | 21     | 18     | -3      | 3      |
| Excel  | 24     | 15     | -9      | 7      |
| PPT    | 19     | 14     | -5      | 4      |
| PDF    | 19     | 16     | -3      | 4      |
| Doc    | 10     | 8      | -2      | 2      |
| **合计** | **93** | **71** | **-22** | **20** |

***

## 二、Word 模块变更（21 -> 18）

> 注：`word_list_documents` 和 `word_copy_document` 保持独立，不合并（查询与文件操作性质不同）。

### W1. 合并 `word_get_info` + `word_analyze_structure` -> `word_get_info`

**原工具：**

- `word_get_info(filename, session_id)` — 返回元信息（title, author, paragraph\_count, table\_count 等）
- `word_analyze_structure(filename, session_id)` — 返回结构分析（heading\_levels, style\_distribution, table\_details, image\_count 等）

**新工具签名：**

```python
def word_get_info(
    filename: str,
    detailed: bool = False,        # 新增：True 时附带结构分析
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- 保留现有 `word_get_info` 逻辑作为基础返回
- 当 `detailed=True` 时，将 `word_analyze_structure` 的逻辑合并进来，追加 `heading_levels`、`style_distribution`、`table_details`、`image_count`、`total_chars` 字段
- 删除独立的 `word_analyze_structure` 函数

**server.py 工具定义：**

```
name: "word_get_info"
description: "获取 Word 文档信息（detailed=true 时附带结构分析：标题层级、样式分布等）"
inputSchema.properties 增加:
  "detailed": _bool_param("是否附带结构分析（标题层级、样式分布、表格详情等）", default=False)
```

### W2. 合并 `word_get_text` + `word_get_outline` -> `word_extract_text`

**原工具：**

- `word_get_text(filename, include_tables, session_id)` — 提取全文文本
- `word_get_outline(filename, session_id)` — 提取标题大纲

**新工具签名：**

```python
def word_extract_text(
    filename: str,
    extract_type: str = "text",    # "text"=全文, "outline"=仅大纲, "all"=两者
    include_tables: bool = True,   # extract_type="text"/"all" 时生效
    session_id: str | None = None,
) -> dict[str, Any]:
```

| extract\_type | 行为                            | 返回字段                                           |
| ------------- | ----------------------------- | ---------------------------------------------- |
| `"text"`      | 提取全文（等同原 `word_get_text`）     | `text`, `paragraph_count`, `tables`            |
| `"outline"`   | 仅提取大纲（等同原 `word_get_outline`） | `outline`                                      |
| `"all"`       | 两者都提取                         | `text`, `paragraph_count`, `tables`, `outline` |

**handler 实现要点：**

- 用 `InputValidator.validate_choice(extract_type, ["text", "outline", "all"], "extract_type")` 校验
- `extract_type="text"` 或 `"all"` 时：复用 `word_get_text` 逻辑
- `extract_type="outline"` 或 `"all"` 时：复用 `word_get_outline` 逻辑
- 删除独立的 `word_get_text` 和 `word_get_outline` 函数

**server.py 工具定义：**

```
name: "word_extract_text"
description: "提取 Word 文本（extract_type: text=全文, outline=仅大纲, all=两者）"
inputSchema.properties:
  "filename": _str_param("文档文件路径")
  "extract_type": _str_param("提取类型（text/outline/all）", default="text")
  "include_tables": _bool_param("是否包含表格文本（text/all 时生效）", default=True)
  "session_id": SESSION_ID_PARAM
inputSchema.required: ["filename"]
```

### W3. 合并 `word_add_page_break` -> `word_add_paragraph`

**原工具：**

- `word_add_page_break(filename, session_id)` — 插入分页符
- `word_add_paragraph(filename, text, style, font_size, bold, session_id)` — 添加段落

**新工具签名：**

```python
def word_add_paragraph(
    filename: str,
    text: str = "",                 # 改为可选（仅插入分页符时可为空）
    style: str | None = None,
    font_size: int | None = None,
    bold: bool = False,
    page_break: bool = False,       # 新增：True 时在段落前插入分页符
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `text` 改为默认空字符串，但校验：`text` 和 `page_break` 至少有一个有效
- 当 `page_break=True` 时，先调用 `doc.add_page_break()`
- 当 `text` 非空时，添加段落（现有逻辑）
- 删除独立的 `word_add_page_break` 函数

**server.py 工具定义：**

```
name: "word_add_paragraph"
description: "添加段落（page_break=true 时同时插入分页符）"
inputSchema.properties 增加:
  "page_break": _bool_param("是否在段落前插入分页符", default=False)
inputSchema.required: ["filename"]  # text 不再是必填
```

***

## 三、Excel 模块变更（24 -> 15）

### E1. 合并 `excel_read_cell` + `excel_read_range` -> `excel_read`

**原工具：**

- `excel_read_cell(filename, sheet, cell_ref, session_id)` — 读单个单元格
- `excel_read_range(filename, sheet, range_str, session_id)` — 读区域

**新工具签名：**

```python
def excel_read(
    filename: str,
    sheet: str,
    range_str: str,                 # 单元格引用（A1）或区域（A1:C10）
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- 统一使用 `range_str` 参数
- 单个单元格（如 "A1"）也作为区域处理，返回 `{"data": [[value]]}`
- 区域（如 "A1:C10"）返回 `{"data": [[...], ...]}`
- 不再需要 `InputValidator.validate_range`（放宽格式允许单单元格）
- 删除 `excel_read_cell` 和 `excel_read_range`

### E2. 合并 `excel_write_cell` + `excel_write_range` -> `excel_write`

**原工具：**

- `excel_write_cell(filename, sheet, cell_ref, value, session_id)` — 写单个单元格
- `excel_write_range(filename, sheet, start_cell, data, session_id)` — 批量写入

**新工具签名：**

```python
def excel_write(
    filename: str,
    sheet: str,
    start_cell: str,                # 起始单元格（如 A1）
    data: list[list[Any]],          # 二维数组（单值写 [[value]]）
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- 统一使用 `start_cell` + `data` 参数
- 写单个值时 `data = [[value]]`
- 复用 `excel_write_range` 的逻辑
- 删除 `excel_write_cell` 和 `excel_write_range`

### E3. 合并 `excel_insert_row` + `excel_delete_row` -> `excel_modify_row`

**新工具签名：**

```python
def excel_modify_row(
    filename: str,
    sheet: str,
    row_idx: int,
    action: str = "insert",         # "insert" | "delete"
    count: int = 1,
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `action="insert"`：调用 `ws.insert_rows(row_idx, count)`
- `action="delete"`：调用 `ws.delete_rows(row_idx, count)`
- 用 `InputValidator.validate_choice(action, ["insert", "delete"], "action")` 校验
- 删除 `excel_insert_row` 和 `excel_delete_row`

### E4. 合并 `excel_insert_column` + `excel_delete_column` -> `excel_modify_column`

**新工具签名：**

```python
def ppt_get_overview(
    filename: str,
    list_slides: bool = False,      # 新增：True 时附带幻灯片概览
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- 同 E3，调用 `ws.insert_cols` / `ws.delete_cols`
- 删除 `excel_insert_column` 和 `excel_delete_column`

### E5. 合并 `excel_add/delete/rename/copy_sheet`（4个） -> `excel_manage_sheet`

**新工具签名：**

```python
def excel_manage_sheet(
    filename: str,
    action: str,                    # "add" | "delete" | "rename" | "copy"
    sheet_name: str | None = None,  # add/delete 时为操作目标；rename 时为旧名称
    new_name: str | None = None,    # rename 时的新名称
    source: str | None = None,      # copy 时的源工作表
    target: str | None = None,      # copy 时的目标工作表名
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `action="add"`：复用 `excel_add_sheet` 逻辑，参数 `sheet_name`
- `action="delete"`：复用 `excel_delete_sheet` 逻辑，参数 `sheet_name`
- `action="rename"`：复用 `excel_rename_sheet` 逻辑，参数 `sheet_name`（旧名）+ `new_name`
- `action="copy"`：复用 `excel_copy_sheet` 逻辑，参数 `source` + `target`
- 用 `InputValidator.validate_choice(action, ["add", "delete", "rename", "copy"], "action")` 校验
- 删除 4 个原函数

### E6. 合并 `excel_list_sheets` -> `excel_get_info`（重命名为 `excel_get_overview`）

**变更说明：**

- `excel_get_info` 已返回 `sheets` 列表（含 name, rows, cols）
- `excel_list_sheets` 仅返回工作表名列表，功能被完全覆盖
- 直接删除 `excel_list_sheets`
- `excel_get_info` 重命名为 `excel_get_overview`（其他模块的 `get_info` 返回 author/title 等元数据，Excel 返回的是工作表结构概览，用 `overview` 更准确）
- handler 函数同步重命名：`excel_get_info` -> `excel_get_overview`

### E7. 移除 `excel_create_pivot_table`

**变更说明：**

- 删除 handler 函数 `excel_create_pivot_table`
- 删除 server.py 中对应的 Tool 定义和路由条目
- LLM 可通过 `excel_read` + 对话内计算 + `excel_write` 替代

***

## 四、PPT 模块变更（19 -> 14）

### P1. 合并 `ppt_analyze_structure` + `ppt_get_structure` -> `ppt_get_structure`

**原工具：**

- `ppt_get_structure(filename, session_id)` — 完整结构树
- `ppt_analyze_structure(filename, session_id)` — 结构分析（内部已调用 `ppt_get_structure`）

**新工具签名：**

```python
def ppt_get_structure(
    filename: str,
    analyze: bool = False,          # 新增：True 时附带统计分析
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- 基础逻辑保留 `ppt_get_structure`（完整结构树）
- 当 `analyze=True` 时，追加 `shape_type_distribution`、`layout_distribution`、`avg_shapes_per_slide` 等分析字段（复用 `ppt_analyze_structure` 逻辑）
- 删除 `ppt_analyze_structure`

### P2. 合并 `ppt_list_slides` -> `ppt_get_info`（重命名为 `ppt_get_overview`）

**原工具：**

- `ppt_get_info(filename, session_id)` - 元信息（slide\_count, 尺寸等）
- `ppt_list_slides(filename, session_id)` - 幻灯片概览列表

**新工具签名：**

```python
def ppt_get_overview(
    filename: str,
    list_slides: bool = False,      # 新增：True 时附带幻灯片概览
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- 保留现有 `ppt_get_info` 逻辑
- 当 `list_slides=True` 时，追加 `slides` 字段（复用 `ppt_list_slides` 逻辑）
- 删除 `ppt_list_slides`
- handler 函数同步重命名：`ppt_get_info` -> `ppt_get_overview`

### P3. 合并 `ppt_delete/move/copy_slide`（3个） -> `ppt_manage_slide`

**新工具签名：**

```python
def ppt_manage_slide(
    filename: str,
    action: str,                    # "delete" | "move" | "copy"
    slide_idx: int,
    new_idx: int | None = None,     # action="move" 时使用
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `action="delete"`：复用 `ppt_delete_slide` 逻辑
- `action="move"`：复用 `ppt_move_slide` 逻辑，额外参数 `new_idx`
- `action="copy"`：复用 `ppt_copy_slide` 逻辑
- 用 `InputValidator.validate_choice(action, ["delete", "move", "copy"], "action")` 校验
- 删除 3 个原函数

### P4. 合并 `ppt_set_slide_notes` + `ppt_get_slide_notes` -> `ppt_slide_notes`

**新工具签名：**

```python
def ppt_slide_notes(
    filename: str,
    slide_idx: int,
    action: str = "get",            # "get" | "set"
    notes_text: str | None = None,  # action="set" 时使用
    session_id: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `action="get"`：复用 `ppt_get_slide_notes` 逻辑
- `action="set"`：复用 `ppt_set_slide_notes` 逻辑，参数 `notes_text`
- 用 `InputValidator.validate_choice(action, ["get", "set"], "action")` 校验
- 删除 `ppt_set_slide_notes` 和 `ppt_get_slide_notes`

***

## 五、PDF 模块变更（19 -> 16）

### F1. 合并 `pdf_encrypt` + `pdf_decrypt` -> `pdf_manage_security`

**新工具签名：**

```python
def pdf_manage_security(
    filename: str,
    action: str,                    # "encrypt" | "decrypt"
    password: str,
    permissions: list[str] | None = None,  # action="encrypt" 时可选
    output: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `action="encrypt"`：复用 `pdf_encrypt` 逻辑
- `action="decrypt"`：复用 `pdf_decrypt` 逻辑
- 删除 `pdf_encrypt` 和 `pdf_decrypt`

### F2. 合并 `pdf_analyze_structure` -> `pdf_get_info`

**原工具：**

- `pdf_get_info(filename)` — 元信息（page\_count, title, author 等）
- `pdf_analyze_structure(filename)` — 结构分析（页面类型、文本密度、表格分布）

**新工具签名：**

```python
def pdf_get_info(
    filename: str,
    analyze: bool = False,          # 新增：True 时附带结构分析
) -> dict[str, Any]:
```

**handler 实现要点：**

- 保留现有 `pdf_get_info` 逻辑
- 当 `analyze=True` 时，追加 `pages` 分析字段（复用 `pdf_analyze_structure` 逻辑）
- 删除 `pdf_analyze_structure`

### F3. `pdf_ocr_text` 替换 OCR 引擎（tesseract -> RapidOCR）

**变更说明：**

- 将 OCR 引擎从 tesseract 替换为 RapidOCR（纯 pip 安装，无系统级依赖）
- `pyproject.toml` 添加依赖：`rapidocr-onnxruntime`
- 移除 `pytesseract` 依赖

**原实现（tesseract）：**
```python
import pytesseract
text = pytesseract.image_to_string(img, lang=lang)
```

**新实现（RapidOCR）：**
```python
from rapidocr_onnxruntime import RapidOCR

ocr = RapidOCR()
result, _ = ocr(img_path)
# result = [[box, text, confidence], ...]
text = "\n".join([item[1] for item in result]) if result else ""
```

**handler 实现要点：**

- `pdf_ocr_text` 函数签名不变，外部行为不变
- 内部将 `pytesseract.image_to_string` 替换为 RapidOCR 调用
- 页面渲染为图片后传给 RapidOCR 识别
- `lang` 参数当前保留但 RapidOCR 默认支持中英文，可忽略该参数
- 移除 `try: import pytesseract` 的导入检查，改为 `from rapidocr_onnxruntime import RapidOCR`

**pyproject.toml 变更：**
```toml
# 移除
# "pytesseract"（如有）

# 新增
"rapidocr-onnxruntime"
```

### F4. 合并 `pdf_add_annotation` + `pdf_add_bookmark` -> `pdf_add_annotation`

**原工具：**

- `pdf_add_annotation(filename, page_idx, annotation_type, content, x, y, output)` — 注释
- `pdf_add_bookmark(filename, title, page_idx, output)` — 书签

**新工具签名：**

```python
def pdf_add_annotation(
    filename: str,
    page_idx: int,
    annotation_type: str,           # 增加 "bookmark" 类型
    content: str,
    x: float = 72,
    y: float = 72,
    output: str | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `annotation_type` 可选值增加 `"bookmark"`
- 当 `annotation_type="bookmark"` 时，`content` 作为书签标题，使用 `writer.add_outline_item(content, page_idx)` 逻辑
- 更新 `InputValidator.validate_choice` 校验列表为 `["highlight", "text", "link", "bookmark"]`
- 删除 `pdf_add_bookmark`

***

## 六、Doc 模块变更（10 -> 8）

### D1. 合并 `doc_register_template` + `doc_delete_template` -> `doc_manage_template`

**新工具签名：**

```python
def doc_manage_template(
    action: str,                    # "register" | "delete"
    name: str,
    format: str | None = None,      # action="register" 时必填
    file_path: str | None = None,   # action="register" 时必填
    description: str = "",
    placeholders: list[str] | None = None,
) -> dict[str, Any]:
```

**handler 实现要点：**

- `action="register"`：复用 `doc_register_template` 逻辑
- `action="delete"`：复用 `doc_delete_template` 逻辑
- 删除 `doc_register_template` 和 `doc_delete_template`

### D2. 合并 `doc_save_session` + `doc_close_session` -> `doc_close_session`

**原工具：**

- `doc_save_session(session_id, output_path)` — 保存 Session
- `doc_close_session(session_id, save)` — 关闭 Session（save=True 时先保存）

**新工具签名：**

```python
def doc_close_session(
    session_id: str,
    save: bool = False,
    output_path: str | None = None,  # 新增：save=True 时指定输出路径
) -> dict[str, Any]:
```

**handler 实现要点：**

- 现有 `doc_close_session` 已有 `save` 参数，内部调用 `doc_save_session`
- 增加 `output_path` 参数，传递给 `doc_save_session`
- `doc_save_session` 保留为内部函数（供 `doc_close_session` 调用），但不再作为独立工具注册
- 从 server.py TOOL\_DEFINITIONS 和 TOOL\_HANDLERS 中移除 `doc_save_session`

> D3 已移除：`doc_get_template_info` 和 `doc_extract_placeholders` 保持独立（读索引 vs 扫描文件，使用场景不同）。

---

## 七、server.py 变更

### 7.1 TOOL\_DEFINITIONS（工具定义列表）

需要同步更新：

- 删除被合并/移除的 Tool 定义
- 新增合并后的 Tool 定义
- 更新合并工具的 `inputSchema`（增加 `action` / `detailed` / `analyze` 等参数）
- 更新文件头注释（第 4 行的工具计数）

### 7.2 TOOL\_HANDLERS（工具路由表）

需要同步更新：

- 删除被合并/移除的路由条目
- 新增合并后的路由条目，指向新的 handler 函数

### 7.3 精简后的完整工具名列表（67 个）

```
Word (18):
  word_create_document, word_get_info, word_extract_text,
  word_list_documents, word_copy_document,
  word_add_heading, word_add_paragraph, word_add_table, word_add_image,
  word_add_list, word_set_header_footer, word_generate_toc,
  word_format_text, word_format_table, word_search_replace,
  word_delete_paragraph, word_create_style, word_extract_tables

Excel (15):
  excel_create_workbook, excel_get_overview, excel_manage_sheet,
  excel_read, excel_write, excel_modify_row, excel_modify_column,
  excel_format_cell, excel_apply_formula, excel_create_chart,
  excel_freeze_panes, excel_sort_data, excel_add_conditional_format,
  excel_analyze_data, excel_find_duplicates

PPT (14):
  ppt_create_presentation, ppt_get_overview, ppt_add_slide, ppt_manage_slide,
  ppt_add_text, ppt_add_image, ppt_add_table, ppt_add_chart, ppt_add_shape,
  ppt_set_background, ppt_apply_theme, ppt_slide_notes,
  ppt_extract_text, ppt_get_structure

PDF (16):
  pdf_get_info, pdf_merge, pdf_split, pdf_rotate_page,
  pdf_extract_text, pdf_extract_tables, pdf_extract_images, pdf_search_text,
  pdf_ocr_text, pdf_add_text, pdf_add_image, pdf_add_watermark, pdf_add_annotation,
  pdf_manage_security, pdf_create_from_template, pdf_fill_form

Doc (8):
  doc_list_templates, doc_get_template_info, doc_manage_template,
  doc_apply_template, doc_extract_placeholders,
  doc_open_session, doc_close_session, doc_list_sessions
```

***

## 八、测试文件更新

### 8.1 需要更新的测试文件

| 测试文件                          | 变更内容             |
| ----------------------------- | ---------------- |
| `tests/test_word_handler.py`  | 更新 W1-W4 涉及的测试用例 |
| `tests/test_excel_handler.py` | 更新 E1-E8 涉及的测试用例 |
| `tests/test_ppt_handler.py`   | 更新 P1-P4 涉及的测试用例 |
| `tests/test_pdf_handler.py`   | 更新 F1-F4 涉及的测试用例 |
| `tests/test_doc_handler.py`   | 更新 D1-D3 涉及的测试用例 |

### 8.2 测试更新原则

- 被合并的旧函数测试 -> 改为调用新合并函数，验证相同行为
- 被移除的工具 -> 删除对应测试用例
- 新增参数（如 `detailed`, `action`, `analyze`）-> 新增测试用例覆盖分支
- 确保 `action` 参数的错误分支也有测试（传入非法 action 值）

***

## 九、实施顺序

按以下顺序逐步实施，每步完成后运行测试确认：

```
第 1 步: Excel 模块（E1-E7，-9 个工具）
  -> 修改 excel_handler.py
  -> 更新 server.py（Excel 部分）
  -> 更新 tests/test_excel_handler.py
  -> 运行测试

第 2 步: Word 模块（W1-W3，-3 个工具）
  -> 修改 word_handler.py
  -> 更新 server.py（Word 部分）
  -> 更新 tests/test_word_handler.py
  -> 运行测试

第 3 步: PPT 模块（P1-P4，-5 个工具）
  -> 修改 ppt_handler.py
  -> 更新 server.py（PPT 部分）
  -> 更新 tests/test_ppt_handler.py
  -> 运行测试

第 4 步: PDF 模块（F1-F4，-3 个工具 + OCR 引擎替换）
  -> 修改 pdf_handler.py
  -> 更新 server.py（PDF 部分）
  -> 更新 tests/test_pdf_handler.py
  -> 运行测试

第 5 步: Doc 模块（D1-D2，-2 个工具）
  -> 修改 doc_handler.py
  -> 更新 server.py（Doc 部分）
  -> 更新 tests/test_doc_handler.py
  -> 运行测试

第 6 步: 全量验证
  -> 运行全部测试
  -> 更新 server.py 文件头注释
  -> 检查 server.py 工具定义与路由表一致性
```

***

## 十、风险与注意事项

1. **工具名称变更**：合并后的工具名称发生变化（如 `word_get_text` -> `word_extract_text`），已有对话中引用旧工具名的场景不受影响（MCP 每次会话重新拉取工具列表）。
2. **参数兼容性**：合并工具的参数结构与原工具不同，LLM 需要根据新的 Tool 定义调整调用方式。Tool 的 `description` 需要清晰描述 `action` 参数的用法。
3. **handler 内部依赖**：部分 handler 之间存在调用关系（如 `ppt_analyze_structure` 内部调用 `ppt_get_structure`，`doc_close_session` 内部调用 `doc_save_session`），合并时需保留内部调用关系。
4. **pandas 依赖**：移除 `excel_create_pivot_table` 后，`excel_analyze_data` 和 `excel_find_duplicates` 仍使用 pandas，`import pandas as pd` 保留。

5. **RapidOCR 依赖**：`pdf_ocr_text` 改用 RapidOCR 后，需在 `pyproject.toml` 添加 `rapidocr-onnxruntime` 依赖。首次使用时会加载 ONNX 模型（约 10-15MB），可能有短暂延迟。
6. **validate\_range 放宽**：`excel_read` 合并后需要同时接受单单元格（"A1"）和区域（"A1:C10"），现有的 `InputValidator.validate_range` 正则 `^[A-Z]+\d+:[A-Z]+\d+$` 不匹配单单元格，需要在 handler 内部处理而非依赖该校验器。

